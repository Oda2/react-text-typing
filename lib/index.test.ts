import { describe, it, expect } from 'vitest';
import TextTyping from './index';

describe('index', () => {
  it('exports TextTyping component', () => {
    expect(TextTyping).toBeDefined();
  });
});
