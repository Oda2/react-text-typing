box: node:dubnium

build:
  steps:
    - script:
      name: install dependencies
      code:
        - yarn install

    - script:
        name: echo nodejs information
        code: |
          - yarn build